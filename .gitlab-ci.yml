image: server-harbor:80/ant/ant_iit:devel

stages:
   - build
   - test

build:
  stage: build
  tags:
    - ant-devel
  script:
    - mkdir build
    - cd build
    - cmake ..
    - make -j$(nproc)
  artifacts:
    paths:
      - build/*
      - bin/*
    expire_in: 3 hrs

test:
  stage: test
  tags: 
    - ant-devel
  script:
    - cd build
    - make -j$(nproc) main_tests
    - ./tests/main_tests
  dependencies:
    - build
